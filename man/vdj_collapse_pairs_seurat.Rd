% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_vdj.R
\name{vdj_collapse_pairs_seurat}
\alias{vdj_collapse_pairs_seurat}
\title{Collapse per-cell TRA/TRB into 1 TRB + up to 2 TRA pairs}
\usage{
vdj_collapse_pairs_seurat(
  seurat,
  tool_key = "vdj",
  drop_multi_trb = TRUE,
  keep_two_alpha = TRUE,
  alpha_support = c("umis", "reads"),
  min_umis = 0L,
  min_reads = 0L,
  require_productive = TRUE,
  require_full_length = FALSE,
  require_high_confidence = FALSE,
  write_meta = TRUE,
  update_primary_meta = TRUE
)
}
\arguments{
\item{seurat}{Seurat v5 object previously processed by \verb{vdj_attach_10x_vdj_v2[_batch]}.}

\item{tool_key}{Character; tools slot name (default "vdj").}

\item{drop_multi_trb}{Logical; drop cells with >1 TRB after QC (default TRUE). If FALSE, picks the best TRB.}

\item{keep_two_alpha}{Logical; if TRUE, produce a secondary pair using TRA2+TRB when available (default TRUE).}

\item{alpha_support}{Character; support metric for ranking alphas: "umis" (default) or "reads".}

\item{min_umis}{Integer; discard contigs below this UMI count (default 0).}

\item{min_reads}{Integer; discard contigs below this read count (default 0).}

\item{require_productive}{Logical; require productive contigs (default TRUE).}

\item{require_full_length}{Logical; require full_length when column exists (default FALSE).}

\item{require_high_confidence}{Logical; require high_confidence when column exists (default FALSE).}

\item{write_meta}{Logical; write metadata columns described above (default TRUE).}

\item{update_primary_meta}{Logical; if TRUE, also set \verb{vdj.tra.*} and \verb{vdj.trb.*} to the primary pair (default TRUE).}
}
\value{
Updated Seurat object (invisible).
}
\description{
Selects one TRB per cell and up to two TRA chains ordered by support, then
emits a \code{pairs} table in \code{obj@tools[[tool_key]]$pairs} with one or two rows
per cell: the dominant pair (TRA1+TRB) and, if present and requested, the
secondary pair (TRA2+TRB).
}
\details{
Selection heuristic:
\itemize{
\item Filter contigs by QC: \code{productive}, \code{full_length}, \code{high_confidence} (configurable).
\item TRB: select exactly one best contig by UMIs, then reads, then high_confidence.
If more than one TRB exists and \code{drop_multi_trb = TRUE}, the cell is dropped.
\item TRA: order candidate alphas by UMIs (or reads) descending, then reads, then
high_confidence; keep the top one (dominant) and optionally the second.
}

Metadata:
\itemize{
\item Writes \verb{vdj.tra1.*}, \verb{vdj.tra2.*} (if present), and \verb{vdj.trb.*} fields per cell when
\code{write_meta = TRUE}. Also writes flags \code{vdj.has_tra2}, \code{vdj.multi_trb}, and \code{vdj.pair_qc}.
\item Optionally updates the primary \verb{vdj.tra.*} and \verb{vdj.trb.*} fields to TRA1/TRB.
}
}
