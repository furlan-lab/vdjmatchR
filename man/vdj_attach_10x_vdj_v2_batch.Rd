% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_vdj.R
\name{vdj_attach_10x_vdj_v2_batch}
\alias{vdj_attach_10x_vdj_v2_batch}
\title{Attach multiple 10x 5' VDJ v2 runs using a caps-token template and prefix map}
\usage{
vdj_attach_10x_vdj_v2_batch(
  seurat,
  prefix_folders,
  vdj_dir_template,
  tool_key = "vdj",
  add_meta = TRUE,
  include_cdr12 = FALSE,
  include_nt_subseq = FALSE,
  expected_t_fraction = 0.5,
  placeholder = "FOLDERNAME",
  prefixes_col = c("prefixes", "PREFIXES"),
  folders_col = "folders",
  cell_regex = NULL,
  cell_map = NULL
)
}
\arguments{
\item{seurat}{A Seurat v5 object.}

\item{prefix_folders}{data.frame with at least a prefix column and one column per UPPERCASE token present in \code{vdj_dir_template}.}

\item{vdj_dir_template}{Character scalar path containing UPPERCASE token(s) such as \code{FOLDERNAME1}, \code{FOLDERNAME2} to be substituted from \code{prefix_folders} per row.}

\item{tool_key}{Name under \code{obj@tools} to store results (default \code{"vdj"}).}

\item{add_meta}{If TRUE, each run writes \code{vdj.clone_id_10x} and \code{vdj.clone_size_10x} for mapped cells.}

\item{include_cdr12}{If TRUE, forward to per-run attach to import CDR1/2 AA where available.}

\item{include_nt_subseq}{If TRUE, forward to per-run attach to include nucleotide sequences for CDR1/2/3 where available.}

\item{expected_t_fraction}{Expected fraction of T cells among queried Seurat cells; forwarded to per-run attach.
A warning is emitted only if \code{(unmatched / queried) > (1 - expected_t_fraction)}. Default: \code{0.5}.}

\item{placeholder}{Token to replace in the template when no UPPERCASE tokens are detected (default \code{"FOLDERNAME"}).}

\item{prefixes_col}{Column name(s) in \code{prefix_folders} to look for the prefix. First found is used (default tries \code{"prefixes"}, then \code{"PREFIXES"}).}

\item{folders_col}{Column name in \code{prefix_folders} for folder names used only in single-placeholder fallback mode (default \code{"folders"}).}

\item{cell_regex}{Optional regex with one capture group to extract barcodes from cell names (forwarded to per-run attach).}

\item{cell_map}{Optional explicit mapping (forwarded to per-run attach).}
}
\value{
Updated Seurat object (invisible).
}
\description{
Convenience wrapper over \code{vdj_attach_10x_vdj_v2()} to batch-attach many
Cell Ranger VDJ outputs to a single Seurat object when cell names were
prefixed to avoid barcode collisions. The user supplies a data.frame with a
prefix column and any number of UPPERCASE columns that correspond to tokens in
a path template. All UPPERCASE tokens present in the template (e.g.,
\code{FOLDERNAME1}, \code{FOLDERNAME2}) are replaced per-row by the values of columns of
the same names to locate each run's VDJ output directory.
}
\details{
Example template with two tokens:
\verb{/path/to/MMCarT_240212_reseq/FOLDERNAME1/data/FOLDERNAME2/outs/per_sample_outs/FOLDERNAME2/vdj_t}

If no UPPERCASE tokens are detected in the template, the function falls back
to single-placeholder mode, replacing \code{placeholder} with values from
\code{folders_col}.
}
