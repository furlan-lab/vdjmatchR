# Match a single clonotype against the database. Returns a list of columns (vector-of-equal-length) suitable for as.data.frame in R.

Match a single clonotype against the database. Returns a list of columns
(vector-of-equal-length) suitable for as.data.frame in R.

## Usage

``` r
match_tcr(db, cdr3, v_segment, j_segment, scope, top_n)
```
