# Filter database by minimum epitope size

Filter database by minimum epitope size

## Usage

``` r
filter_db_by_epitope_size(db, min_size = 1L)
```

## Arguments

- db:

  an RDatabase object

- min_size:

  minimum number of unique CDR3s per epitope

## Value

an RDatabase object
